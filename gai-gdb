# gdb -nx --batch -x ./gai-gdb -f <file> | sed 's/^Z//g' | sed 's/\t/  /g' > gdb.out 

set disassemble-next-line on
set disassembly-flavor intel

b _start.gai_s
run

while 1
  info registers
  stepi
end

quit
